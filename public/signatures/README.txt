Astrologer digital signature for the Prescription Pad PDFs.

Put a TRANSPARENT PNG here named exactly:

    rahul-raj-sign.png

Every generated PDF (Print / Save / Share / WhatsApp) automatically renders this
signature just above the "संपर्क समय" footer (bottom-right). No upload or
selection is needed. To change the signature later, simply replace this file with
another PNG of the SAME name — all future PDFs use the new one automatically.

Position / size are tuned in lib/prescriptionPad/config.ts → SIGNATURE
(xMm, yMm, widthMm). Height scales proportionally; transparency is preserved.
