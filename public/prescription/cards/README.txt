Prescription Module — selection card images.

Put the THREE provided card artwork files here with these EXACT names
(PNG, transparent background preserved):

    basic.png          → Basic Kundali card
    kundali-milan.png  → Kundali Milan card
    birth-child.png    → Birth Child Kundali card

The selection screen (app/components/prescription/PrescriptionModule.tsx) shows
these as the main card visuals. If a file is missing, the card safely falls back
to an emoji so the layout never breaks. Replace with the SAME filenames anytime
to update the artwork — no code change needed.
